<svg width="350" height="425" viewBox="0 0 350 425" xmlns="http://www.w3.org/2000/svg">
  <style>
    .card-bg { fill: #0d1117; stroke: #30363d; stroke-width: 1px; rx: 12px; }
    .header-title { fill: #58a6ff; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; font-size: 15px; font-weight: 600; }
    .lang-name { fill: #c9d1d9; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; font-size: 13px; font-weight: 500; }
    .lang-pct { fill: #8b949e; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; font-size: 12px; text-anchor: end; font-weight: 500; }

    .lang-row { transition: transform 0.2s ease; }
    .lang-row:hover .bar { filter: brightness(1.3); }
    .lang-row:hover .lang-name { fill: #58a6ff; }

    
    @keyframes scaleBar0 {
      from { transform: scaleX(0); }
      to { transform: scaleX(1); }
    }
    .bar-0 {
      transform-origin: left;
      animation: scaleBar0 0.8s cubic-bezier(0.25, 1, 0.5, 1) forwards;
    }
  

    @keyframes scaleBar1 {
      from { transform: scaleX(0); }
      to { transform: scaleX(1); }
    }
    .bar-1 {
      transform-origin: left;
      animation: scaleBar1 0.8s cubic-bezier(0.25, 1, 0.5, 1) forwards;
    }
  

    @keyframes scaleBar2 {
      from { transform: scaleX(0); }
      to { transform: scaleX(1); }
    }
    .bar-2 {
      transform-origin: left;
      animation: scaleBar2 0.8s cubic-bezier(0.25, 1, 0.5, 1) forwards;
    }
  

    @keyframes scaleBar3 {
      from { transform: scaleX(0); }
      to { transform: scaleX(1); }
    }
    .bar-3 {
      transform-origin: left;
      animation: scaleBar3 0.8s cubic-bezier(0.25, 1, 0.5, 1) forwards;
    }
  

    @keyframes scaleBar4 {
      from { transform: scaleX(0); }
      to { transform: scaleX(1); }
    }
    .bar-4 {
      transform-origin: left;
      animation: scaleBar4 0.8s cubic-bezier(0.25, 1, 0.5, 1) forwards;
    }
  

    @keyframes scaleBar5 {
      from { transform: scaleX(0); }
      to { transform: scaleX(1); }
    }
    .bar-5 {
      transform-origin: left;
      animation: scaleBar5 0.8s cubic-bezier(0.25, 1, 0.5, 1) forwards;
    }
  

    @keyframes scaleBar6 {
      from { transform: scaleX(0); }
      to { transform: scaleX(1); }
    }
    .bar-6 {
      transform-origin: left;
      animation: scaleBar6 0.8s cubic-bezier(0.25, 1, 0.5, 1) forwards;
    }
  

    @keyframes scaleBar7 {
      from { transform: scaleX(0); }
      to { transform: scaleX(1); }
    }
    .bar-7 {
      transform-origin: left;
      animation: scaleBar7 0.8s cubic-bezier(0.25, 1, 0.5, 1) forwards;
    }
  
  </style>

  <rect width="100%" height="100%" class="card-bg" />
  <text x="25" y="30" class="header-title">Lenguajes más usados</text>
  
      <g class="lang-row" transform="translate(25, 55)">
        <circle cx="6" cy="-4" r="5" fill="#f1e05a" />
        <text x="20" y="0" class="lang-name">JavaScript</text>
        <text x="300" y="0" class="lang-pct">27.6%</text>

        <!-- Pista de fondo de la barra -->
        <rect x="0" y="8" width="270" height="7" rx="3.5" fill="#21262d" />

        <!-- Barra de progreso con width explícito + animación por escalado -->
        <rect class="bar bar-0" x="0" y="8" width="74.6" height="7" rx="3.5" fill="#f1e05a" />
      </g>
    
      <g class="lang-row" transform="translate(25, 100)">
        <circle cx="6" cy="-4" r="5" fill="#3572A5" />
        <text x="20" y="0" class="lang-name">Python</text>
        <text x="300" y="0" class="lang-pct">22.8%</text>

        <!-- Pista de fondo de la barra -->
        <rect x="0" y="8" width="270" height="7" rx="3.5" fill="#21262d" />

        <!-- Barra de progreso con width explícito + animación por escalado -->
        <rect class="bar bar-1" x="0" y="8" width="61.5" height="7" rx="3.5" fill="#3572A5" />
      </g>
    
      <g class="lang-row" transform="translate(25, 145)">
        <circle cx="6" cy="-4" r="5" fill="#222c37" />
        <text x="20" y="0" class="lang-name">ShaderLab</text>
        <text x="300" y="0" class="lang-pct">14.3%</text>

        <!-- Pista de fondo de la barra -->
        <rect x="0" y="8" width="270" height="7" rx="3.5" fill="#21262d" />

        <!-- Barra de progreso con width explícito + animación por escalado -->
        <rect class="bar bar-2" x="0" y="8" width="38.6" height="7" rx="3.5" fill="#222c37" />
      </g>
    
      <g class="lang-row" transform="translate(25, 190)">
        <circle cx="6" cy="-4" r="5" fill="#663399" />
        <text x="20" y="0" class="lang-name">CSS</text>
        <text x="300" y="0" class="lang-pct">13.4%</text>

        <!-- Pista de fondo de la barra -->
        <rect x="0" y="8" width="270" height="7" rx="3.5" fill="#21262d" />

        <!-- Barra de progreso con width explícito + animación por escalado -->
        <rect class="bar bar-3" x="0" y="8" width="36.1" height="7" rx="3.5" fill="#663399" />
      </g>
    
      <g class="lang-row" transform="translate(25, 235)">
        <circle cx="6" cy="-4" r="5" fill="#e34c26" />
        <text x="20" y="0" class="lang-name">HTML</text>
        <text x="300" y="0" class="lang-pct">11.0%</text>

        <!-- Pista de fondo de la barra -->
        <rect x="0" y="8" width="270" height="7" rx="3.5" fill="#21262d" />

        <!-- Barra de progreso con width explícito + animación por escalado -->
        <rect class="bar bar-4" x="0" y="8" width="29.8" height="7" rx="3.5" fill="#e34c26" />
      </g>
    
      <g class="lang-row" transform="translate(25, 280)">
        <circle cx="6" cy="-4" r="5" fill="#178600" />
        <text x="20" y="0" class="lang-name">C#</text>
        <text x="300" y="0" class="lang-pct">6.1%</text>

        <!-- Pista de fondo de la barra -->
        <rect x="0" y="8" width="270" height="7" rx="3.5" fill="#21262d" />

        <!-- Barra de progreso con width explícito + animación por escalado -->
        <rect class="bar bar-5" x="0" y="8" width="16.4" height="7" rx="3.5" fill="#178600" />
      </g>
    
      <g class="lang-row" transform="translate(25, 325)">
        <circle cx="6" cy="-4" r="5" fill="#aace60" />
        <text x="20" y="0" class="lang-name">HLSL</text>
        <text x="300" y="0" class="lang-pct">3.0%</text>

        <!-- Pista de fondo de la barra -->
        <rect x="0" y="8" width="270" height="7" rx="3.5" fill="#21262d" />

        <!-- Barra de progreso con width explícito + animación por escalado -->
        <rect class="bar bar-6" x="0" y="8" width="8.0" height="7" rx="3.5" fill="#aace60" />
      </g>
    
      <g class="lang-row" transform="translate(25, 370)">
        <circle cx="6" cy="-4" r="5" fill="#3178c6" />
        <text x="20" y="0" class="lang-name">TypeScript</text>
        <text x="300" y="0" class="lang-pct">0.9%</text>

        <!-- Pista de fondo de la barra -->
        <rect x="0" y="8" width="270" height="7" rx="3.5" fill="#21262d" />

        <!-- Barra de progreso con width explícito + animación por escalado -->
        <rect class="bar bar-7" x="0" y="8" width="4.0" height="7" rx="3.5" fill="#3178c6" />
      </g>
    
</svg>